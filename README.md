
🍔 Проект FOOD — Калькулятор Бургеров

Интерактивное веб-приложение для выбора бургеров с динамическим расчетом стоимости, калорий и анимированным интерфейсом.
(Технологии и методы)

 В этом проекте я реализовал несколько ключевых концепций JavaScript:

* DOM Manipulation: Активная работа с методами querySelector, querySelectorAll, closest и атрибутами data-* для связи HTML с логикой JS.

* Рекурсия (Recursion): Создана функция lvlAnimation, которая вызывает саму себя через setTimeout. Это позволило реализовать плавный счетчик "LVL" в шапке сайта с динамически изменяющейся скоростью.

* Геттеры (Getters): В объекте products использованы функции-геттеры (get summ(), get summKcall()). Это позволило автоматически вычислять итоговую стоимость и калории в зависимости от количества (amount) и выбранных допов, не пересчитывая их вручную каждый раз.

* Event Delegation & Handling: Обработка кликов по кнопкам добавления/удаления/сброса и изменениям чекбоксов.

* Объектно-ориентированный подход: Хранение всех данных о продуктах и ингредиентах в структурированных объектах для легкого масштабирования.
